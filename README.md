# Electable
## Made for BeaverHacks 2026

## Setup

Install dependencies via `npm install`.
Install Redis for your system.

## Running

Start the Redis server with redis-server (on default port 6379).
Use `npm run dev` to serve the project.

To shut down run `shutdown` in `redis-cli` and stop the development server.